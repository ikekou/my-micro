import type { MetaFunction } from "react-router";
import { socialMeta } from "../lib/social-meta";
import { Link, useLoaderData } from "react-router";
import { env } from "cloudflare:workers";
import { REPOSITORY_URL } from "@my-micro/shared";
import { useLocale, words, localHref } from "../lib/i18n";
import { BackLink } from "../components/site-shell";

export const meta: MetaFunction = (args) => socialMeta(args, { title: "About & privacy — My Micro" });
export function loader() {
  let supportUrl: string | null = null;
  try { const url = new URL(env.SUPPORT_URL); if (url.protocol === "https:" || url.protocol === "mailto:") supportUrl = url.href; } catch { /* A public contact is configured before launch, never inferred. */ }
  return { supportUrl };
}
export default function About() {
  const locale = useLocale();
  const { supportUrl } = useLoaderData<typeof loader>();
  return <article className="narrow-page prose-page"><BackLink /><header className="page-heading"><span className="eyebrow">ABOUT MY MICRO</span><h1>{words(locale, "Made for the way you work.", "それぞれの使い方を、眺める場所。")}</h1><p>{words(locale, "My Micro is an independent community gallery for Codex Micro setups.", "My Microは、Codex Microの設定を共有する個人運営のコミュニティギャラリーです。")}</p></header><section><h2>{words(locale, "What becomes public", "公開されるもの")}</h2><p>{words(locale, "Your post title, description, keycap layout, assigned function names, supported options, capture date, and Codex app version are public. Your GitHub username and author profile are associated with your posts.", "投稿のタイトル、説明、キーキャップ配置、割り当てた機能名、対応するオプション、取得日時、Codexのアプリ版を公開します。投稿にはGitHubのユーザー名と作者ページが紐づきます。")}</p><p>{words(locale, "A custom skill's name can be public. Local paths, skill source code, saved text content, chat names and IDs, and credentials are excluded by the collector.", "独自スキルは名前が公開されることがあります。収集プログラムは、ローカルパス、スキル本体、定型文の本文、会話名・ID、認証情報を除外します。")}</p></section><section><h2>{words(locale, "Your review comes first", "公開前に、内容を確認")}</h2><p>{words(locale, "The plugin prepares a local snapshot and shows the public content in Codex. It sends that snapshot only after you confirm. Connecting your account by code is a separate step and does not publish anything.", "プラグインは公開用のデータをMac内に作り、Codexで内容を見せます。あなたが確認してから、同じ内容を送信します。コードを使った接続認証だけでは、投稿は公開されません。")}</p></section><section><h2>{words(locale, "Sign-in and cookies", "ログインとCookie")}</h2><p>{words(locale, "GitHub provides account information for sign-in. My Micro stores your GitHub account ID, email address, and the connection information needed to identify you and protect your posts, including session creation and expiry dates. Your email and session credentials are not public. Cookies keep you signed in and remember your language choice.", "GitHubからログイン用のアカウント情報を受け取り、本人の識別と投稿管理のために、GitHubのアカウントID・メールアドレスや、接続の作成日時・有効期限などを保存します。メールアドレスや接続の認証情報は公開しません。Cookieはログインの維持と言語設定の記憶に使います。")}</p><p>{words(locale, "My Micro does not use your Codex or ChatGPT login credentials.", "My MicroがCodexやChatGPTの認証情報を取得・流用することはありません。")}</p></section><section><h2>{words(locale, "Manage your posts and connections", "投稿と接続を管理する")}</h2><p>{words(locale, "You can update or delete a post through Codex. You can also delete it directly from My posts in your browser. Deleting removes it from the public gallery; it does not change your Micro settings.", "Codexから投稿を更新・削除できます。ブラウザの「自分の投稿」から直接削除することもできます。削除するとギャラリーから表示されなくなり、Micro本体の設定は変わりません。")}</p><p><Link to={localHref("/me/posts", locale)}>{words(locale, "My posts", "自分の投稿")}</Link> · <Link to={localHref("/me/account", locale)}>{words(locale, "Account & connections", "アカウントと接続")}</Link></p></section><section><h2>{words(locale, "Open source", "オープンソース")}</h2><p>{words(locale, "The website and plugin source are available on GitHub. The code's MIT license applies to the code, not to people's posts. Codex Micro is a collaboration between OpenAI and Work Louder; this gallery is independently operated.", "サイトとプラグインのソースはGitHubで公開しています。コードのMITライセンスは、投稿内容の利用条件を定めるものではありません。Codex MicroはOpenAIとWork Louderの製品で、このギャラリーは独立して運営しています。")}</p><a href={REPOSITORY_URL} target="_blank" rel="noreferrer">{words(locale, "View the project on GitHub", "GitHubでプロジェクトを見る")}</a></section>{supportUrl && <section><h2>{words(locale, "Get in touch", "お問い合わせ")}</h2><p>{words(locale, "For questions about your posts, data, or a problem with the gallery, contact the project maintainer.", "投稿・データについてのご相談や、ギャラリーの不具合は、運営者までご連絡ください。")}</p><a href={supportUrl} rel="noreferrer">{words(locale, "Contact the maintainer", "運営者に問い合わせる")}</a></section>}</article>;
}
